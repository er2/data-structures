apply plugin: 'java'

group 'com.tspartnersinc'
version '1'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

// In this section you declare the dependencies for your production and test code
dependencies {
    compile 'org.junit.jupiter:junit-jupiter-api'
}
